<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mypetstore_back_management.persistence.OrderMapper">
    <select id="getAllOrder" resultType="com.example.mypetstore_back_management.domain.Order">
        select
            orders.orderid,
            orders.userid,
            orders.totalprice,
            orders.orderdate,
            orders.shipaddr1,
            orders.shipaddr2,
            orders.shipcity,
            orders.delivery
        from
            orders
    </select>

    <insert id="insertOrder" >

    </insert>
    <delete id="deleteLineItem">
        delete
        from lineitem
        where orderid = #{order.orderid}
    </delete>
    <delete id="deleteOrder">
    delete
    from orders
    where orderid=#{order.orderid}
    </delete>
    <select id="getOrderByOrderId" parameterType="com.example.mypetstore_back_management.domain.Order" resultType="com.example.mypetstore_back_management.domain.Order">
        select
            orders.orderid,
            orders.userid,
            orders.totalprice,
            orders.orderdate,
            orders.shipaddr1,
            orders.shipaddr2,
            orders.shipcity,
            orders.delivery
        from orders
        where orderid=#{orderid}
    </select>
    <select id="getOrderByUserId" parameterType="string" resultType="com.example.mypetstore_back_management.domain.Order">
        select
            orders.orderid,
            orders.userid,
            orders.totalprice,
            orders.orderdate,
            orders.shipaddr1,
            orders.shipaddr2,
            orders.shipcity,
            orders.delivery
        from orders
        where userid=#{userid}
    </select>
    <update id="updateOrder" parameterType="com.example.mypetstore_back_management.domain.Order">
        update
          orders
        set
        shipaddr1=#{order.shipaddr1}
        , shipaddr2=#{order.shipaddr2}
        , shipcity=#{order.shipcity}
        where orderid=#{order.orderid}
    </update>
    <update id="updatedelivery">
        update
           orders
        set delivery = 1
        where orderid=#{orderid}
    </update>
</mapper>